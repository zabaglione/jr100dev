Switch between “crates are walls” and “enemies are walls” to reach the diamond exit across 40 puzzles. Later rooms combine rule-switching positions with the order in which you visit branches. Detours for circular runes are optional.

## Controls and play

WASD moves and RETURN switches the passage rule. Each successful one-cell move and each rule switch counts as one action. Attempting an impassable cell does not count. Entering the exit clears immediately, so collect both runes first if you want three stars.

<!-- common-controls -->

The explorer faces the direction of travel and moves through intermediate positions with sound. Wait for the animation to finish before pressing again.

The RULE TABLE arrow marks the active wall rule. REWRITES counts rule changes. MOV is moves plus switches, PAR is the target, and RUNES counts optional runes collected.

<!-- ranked-campaign -->
