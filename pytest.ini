[pytest]
markers =
    slow: marks tests as slow-running integration checks
    integration: marks integration tests building full samples
    order(name): provides execution ordering hint (requires pytest-order plugin)
